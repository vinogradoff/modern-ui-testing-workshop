pipeline {
    agent any

    options { timestamps () }

    stages {
        stage("run tests") {
            steps {
                echo "running test"
                sh "cd 03_jenkins101; ./gradlew test"
            }
        }
        stage("reports") {
            steps {
                echo "running reports"
            }
        }
    }
}
