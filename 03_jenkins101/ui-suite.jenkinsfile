pipeline {
    agent any

    options { timestamps () }

    stages {
        stage("run tests ") {
            steps {
                echo "running test"
                sh 'gradle test'
            }
        }
        stage("reports") {
            steps {
                echo "running reports"
            }
        }
    }
}